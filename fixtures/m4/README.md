# M4 synthetic fixtures

These fixtures are `test_fixture` representations only. They contain no raw
official content, source URLs, or canonical evidence and do not represent a
completed 30-day operational rehearsal. `partial-window.ts` intentionally
stops after five observed days and therefore must reduce to
`insufficient_data`.
